% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Diagnostic.R
\name{diagn_IME_RQUMW}
\alias{diagn_IME_RQUMW}
\title{Information Matrix Equality diagnostics for RQ-UMW models}
\usage{
diagn_IME_RQUMW(
  fit,
  M = c(1, 2, 3),
  base_size = 12,
  graphic = T,
  print = T,
  act_parallel = T,
  g_num = T,
  n_cores = (parallel::detectCores() - 1)
)
}
\arguments{
\item{fit}{An object of class \code{RQUMW} returned by \code{fit_RQUMW()}.}

\item{M}{Integer vector indicating which diagnostics to compute:
\code{1} for \eqn{D_t}, \code{2} for \eqn{S_{3,t}} and \code{3} for \eqn{S_{5,t}}.}

\item{base_size}{Base font size for diagnostic plots.}

\item{graphic}{Logical; if \code{TRUE}, diagnostic plots are produced.}

\item{print}{Logical; if \code{TRUE}, results are printed to the console.}

\item{act_parallel}{Logical; if \code{TRUE}, parallel computation is used.}

\item{g_num}{Logical; if \code{TRUE}, observation indices are displayed in plots.}

\item{n_cores}{Number of cores to be used in parallel computation.}
}
\value{
A list containing:
\itemize{
\item diagnostic measures (\eqn{D_t}, \eqn{S_{3,t}}, \eqn{S_{5,t}});
\item identified influential observations;
\item information matrix components;
\item diagnostic plots (if requested).
}
}
\description{
Computes case-deletion diagnostics based on the Information Matrix Equality
(IME) for RQ-UMW models, including the statistics
\eqn{D_t}, \eqn{S_{3,t}} and \eqn{S_{5,t}}.
}
\examples{
library(UMW)

set.seed(25)
X <- runif(100, 0, 1)
y <- 0.3 + 0.7*X + rnorm(100, 0, 0.05)
y <- pmin(pmax(y, 0.01), 0.99)
fit<-fit_RQUMW(f = y ~ X)

diagn_IME_RQUMW(fit,M = c(1,2,3))

diagn_IME_RQUMW(fit,M = c(2,3),g_num = FALSE)

}
