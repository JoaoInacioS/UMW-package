% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Diagnostic.R
\name{diagn_LocI_RQUMW}
\alias{diagn_LocI_RQUMW}
\title{Local influence diagnostics for RQ-UMW models}
\usage{
diagn_LocI_RQUMW(
  fit,
  M = c(1, 2),
  k = 0,
  g_num = T,
  base_size = 12,
  print = T,
  graphic = T
)
}
\arguments{
\item{fit}{An object of class \code{RQUMW} returned by \code{fit_RQUMW()}.}

\item{M}{Integer vector indicating which diagnostics to compute:
\code{1} for \eqn{C_t} and \code{2} for \eqn{I_{max}}.}

\item{k}{Index of the parameter to be perturbed. Use \code{0} for all parameters.}

\item{g_num}{Logical; if \code{TRUE}, observation indices are displayed in plots.}

\item{base_size}{Base font size for diagnostic plots.}

\item{print}{Logical; if \code{TRUE}, results are printed to the console.}

\item{graphic}{Logical; if \code{TRUE}, diagnostic plots are produced.}
}
\value{
A list containing:
\itemize{
\item diagnostic measures (\eqn{C_t}, \eqn{I_{max}})
\item local influence measures;
\item identified influential observations;
\item diagnostic plots (if requested).
}
}
\description{
Computes local influence diagnostics for RQ-UMW models using case-weight
perturbation schemes, including the curvature measure \eqn{C_t}
and the maximum normal curvature direction \eqn{I_{max}}.
}
\examples{
library(UMW)

set.seed(25)
X <- runif(100, 0, 1)
y <- 0.3 + 0.7*X + rnorm(100, 0, 0.05)
y <- pmin(pmax(y, 0.01), 0.99)
fit<-fit_RQUMW(f = y ~ X)

UMW::diagn_LocI_RQUMW(fit,M = c(1,2),k = 0)

UMW::diagn_LocI_RQUMW(fit,M = c(1),k = 2,g_num = F)


}
