% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Gera_simu_RUMW_multi.R
\name{simulate_RQUMW}
\alias{simulate_RQUMW}
\title{Monte Carlo Simulation Study for the Regression Quantile Unit-Modified Weibull (RQ-UMW) Model}
\usage{
simulate_RQUMW(
  f = y ~ X1 + X2 | 1,
  theta = c(0.5, -0.6, 0.2, 1.5, 2.3),
  method = "BFGS",
  n = 50,
  re = 100,
  RF = 100,
  tau = 0.5,
  link_mu = "probit",
  save = F,
  start.theta = NULL,
  n_cores = (parallel::detectCores() - 1),
  cen_name = "sim",
  set_seed = NULL
)
}
\arguments{
\item{f}{A model formula that specifies the covariate structure of the RQUMW
model.}

\item{theta}{Numeric vector containing the true parameter values used
in the data-generating process.}

\item{method}{Optimization method used by \code{\link[stats]{optim}}.
Possible values are \code{"Nelder-Mead"}, \code{"BFGS"},
\code{"CG"} and \code{"SANN"}.}

\item{n}{Integer vector indicating the sample sizes to be considered.}

\item{re}{Integer indicating the number of Monte Carlo replications.}

\item{RF}{Integer number indicating the desired number of replicates.}

\item{tau}{Numeric vector with quantile levels in the interval \eqn{(0,1)}.}

\item{link_mu}{Character string or function specifying the link function
for the location parameter. If a character string is provided, it must be
one of \code{"logit"}, \code{"probit"}, \code{"cauchit"},
\code{"loglog"}, or \code{"cloglog"}. Alternatively, a custom link
function may be supplied as an object of class \code{"function"}.}

\item{save}{Logical; if \code{TRUE}, the simulation results are saved to disk.}

\item{start.theta}{Optional numeric vector of initial values for the
parameters. If \code{NULL}, default values are internally
computed.}

\item{n_cores}{Integer indicating the number of CPU cores to be used
for parallel computation.}

\item{cen_name}{Character string used as a prefix for saved files.}

\item{set_seed}{Integer. Optional random seed for reproducibility.}
}
\value{
A list with the following components:
\itemize{
\item \code{sample}: generated samples from the RQUMW model for each
combination of \code{n} and \code{tau};
\item \code{sim}: a list containing the results of the Monte Carlo study,
with the following elements:
\itemize{
\item \code{output}: a numeric matrix of parameter estimates, where
each row corresponds to one Monte Carlo replication and each column
corresponds to one model parameter;
\item \code{table}: a numeric matrix (or data frame) summarizing the
Monte Carlo results, including the true parameter values, empirical
mean, bias, mean squared error (MSE), asymmetry (AS) and kurtosis (K).
This table is printed to the console as a side effect of the function;
\item \code{no_converg}: an integer giving the number of Monte Carlo
replications for which the estimation algorithm did not converge;
\item \code{time}: a character string reporting the total computation
time of the Monte Carlo study.
\item \code{cen_cov}: generated covariates used in the simulations;
\item \code{link_mu}: the link function specification used for the
\eqn{\mu} parameter.
}}
}
\description{
Conducts a Monte Carlo simulation study for the (RQ-UMW) model under different
sample sizes and quantile levels.
}
\examples{
library(UMW)

# Example without saving
f1<-y~X|Z
theta1<-c(beta=c(-2,4),gamma=c(7),delta=c(-0.5,5.4))

simulate_RQUMW(f = f1,theta = theta1,n = c(100),tau = c(0.5),link_mu = "probit",
               re = 1100,RF = 1000,method = "BFGS",set_seed = 25)

# Example saving
f2<-y~X1 + X2|Z
theta2<-c(beta=c(-2,0.7,3),gamma=c(2.6),delta=c(1.5,3.4))

simulate_RQUMW(f = f2,theta = theta2,n = c(100,300),tau = c(0.25,0.5),re = 1100,
               RF = 1000,save = T,cen_name = paste0("sim_cen2"),
               method = "BFGS",set_seed = 25)

}
