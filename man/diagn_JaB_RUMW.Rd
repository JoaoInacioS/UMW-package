% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Diagnostic.R
\name{diagn_JaB_RUMW}
\alias{diagn_JaB_RUMW}
\title{Jackknife-after-Bootstrap influence diagnostics for RQ-UMW models}
\usage{
diagn_JaB_RUMW(
  fit,
  M = c(1, 2, 3, 4, 5, 6, 7),
  B = 200,
  graphic = T,
  set_seed = NULL,
  save = F,
  name_JaB = "JaB",
  n_cores = (parallel::detectCores() - 1)
)
}
\arguments{
\item{fit}{An object of class \code{RQUMW} returned by \code{fit_RQUMW()}.}

\item{M}{Integer vector indicating which influence measures to evaluate:
\code{1} C_t, \code{2} I_max, \code{3} D_t, \code{4} S3_t,
\code{5} S5_t, \code{6} Frèchet, \code{7} Rao.}

\item{B}{Number of bootstrap replications.}

\item{graphic}{Logical; if \code{TRUE}, diagnostic plots are produced.}

\item{set_seed}{Optional integer to set a random seed for reproducibility.}

\item{save}{Logical; if \code{TRUE}, results are saved to disk.}

\item{name_JaB}{Character string used as prefix for saved objects.}

\item{n_cores}{Number of cores to be used in parallel computation.}
}
\value{
A list containing, for each selected measure:
\itemize{
\item original influence measure;
\item bootstrap distributions (\eqn{Fstar});
\item JaB influence values (\eqn{Fstar_i});
\item identified influential observations;
\item computation time.
}
}
\description{
Computes Jackknife-after-Bootstrap (JaB) influence diagnostics for several
influence measures derived from RQ-UMW models.
}
\examples{
library(UMW)

set.seed(25)
X <- runif(100, 0, 1)
y <- 0.3 + 0.7*X + rnorm(100, 0, 0.05)
y <- pmin(pmax(y, 0.01), 0.99)
fit<-fit_RQUMW(f = y ~ X)

diagn_JaB_RUMW(fit,M = 1,B = 200,set_seed = 25)

diagn_JaB_RUMW(fit,M = c(1:7),B = 200,set_seed = 25,save = T,name_JaB = "JaB")


}
